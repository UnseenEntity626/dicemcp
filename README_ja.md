# Dice MCP Server

TRPGセッション用のシンプルなダイスMCPサーバーです。

[English](README.md) | 日本語

## 機能

- **xDy形式のダイスロール**: 任意の個数・面数のダイスを振れます
- **目標値判定**: 目標値を指定すると成功/失敗判定を行います
- **修正値対応**: 合計値に加算される修正値を指定できます
- **コインフリップ**: 表か裏を返します

## インストール

```bash
git clone https://github.com/yourusername/dicemcp.git
cd dicemcp
npm install
npm run build
```

## 使用方法

MCPクライアントの設定に追加してください：

### Gemini CLI (`~/.gemini/settings.json`)

```json
{
  "mcpServers": {
    "dicemcp": {
      "command": "node",
      "args": ["/path/to/dicemcp/dist/index.js"]
    }
  }
}
```

### Claude Desktop (`claude_desktop_config.json`)

```json
{
  "mcpServers": {
    "dicemcp": {
      "command": "node",
      "args": ["/path/to/dicemcp/dist/index.js"]
    }
  }
}
```

## ツール

### roll_dice

xDy形式でダイスを振ります。目標値を指定すると成功/失敗の判定も行います。

| パラメータ | 型 | 必須 | 説明 |
|-----------|------|------|------|
| count | number | ✅ | ダイスの個数 (1-100) |
| sides | number | ✅ | ダイスの面数 (2-1000) |
| target | number | | 目標値 |
| modifier | number | | 修正値 |

**使用例:**

- 2d6を振る: `count: 2, sides: 6`
- 1d20+5で目標値15判定: `count: 1, sides: 20, target: 15, modifier: 5`

### flip_coin

コインを投げて表(heads)か裏(tails)を返します。パラメータは不要です。

## 出力例

```
🎲 Rolled 2d6+3
━━━━━━━━━━━━━━━━━━━━
Dice: [4, 5]
Total: 9 +3 = 12
━━━━━━━━━━━━━━━━━━━━
Target: 10
✅ Success! (+2)
```

```
🪙 Coin flip: heads
```

## ライセンス

MIT
